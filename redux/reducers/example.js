const initialState = {
    msg: 'IT WORKS!!!!!!',
    count: 0
}

export default function example(state = initialState, action) {
    switch (action.type) {
        default:
            return state
    }
}